services:
  cenam:
    build: .
    container_name: cenam-app
    ports:
      - "8501:8501"
    volumes:
      - cenam-data:/app/data
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

volumes:
  cenam-data:
